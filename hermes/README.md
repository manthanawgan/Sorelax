# Hermes × Sorelax

See the main [README](../README.md) for overview. Post-install:

```bash
hermes model
bash hermes/register_cron.sh   # if not run by install.sh
hermes gateway
hermes cron run <job_id>
```

MCP: copy `claude_code_mcp_config.json` to `~/.claude/claude_code_mcp_config.json`.
